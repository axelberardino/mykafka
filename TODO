TODO

* Instead of a binary search, try a direct access through index (because the
  offset "contiguous + base_offser" is known) at the index level.

* Compare perfs with:
  https://softwaremill.com/mqperf/#kafka
